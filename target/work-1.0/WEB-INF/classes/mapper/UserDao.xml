<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.dao.UserDao">

    <resultMap id="UserMap" type="User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
    </resultMap>

    <resultMap id="User2Map" type="User2">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
    </resultMap>

    <select id="checkUser" parameterType="User2" resultMap="User2Map">
        select * from "user" where username = #{username} and password = #{password}
    </select>

    <insert id="save" parameterType="User">
        insert into work_user (name, email) values (#{name}, #{email})
    </insert>

    <update id="update" parameterType="User">
        update work_user set name = #{name}, email = #{email} where id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        delete from work_user where id = #{id}
    </delete>

    <select id="count" parameterType="MiniRequest" resultType="int">
        select count(id) from work_user wu
        <where>
            <if test="key != null">
                <bind name="pattern" value="'%' + key + '%'"/>
                wu.name like #{pattern}
            </if>
        </where>
    </select>

    <select id="getUsers" parameterType="MiniRequest" resultMap="UserMap">
        select * from (select *, row_number() over (order by id) rn from work_user ) t
        <where>
            t.rn BETWEEN #{start} and #{end}
            <if test="key != null">
                <bind name="pattern" value="'%' + key + '%'"/>
                and t.name like #{pattern}
            </if>
        </where>
    </select>

</mapper>